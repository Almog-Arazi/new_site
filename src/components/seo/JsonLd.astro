---
import { safeJsonLd } from '../../lib/jsonld';

interface Props {
  data: unknown;
}

const { data } = Astro.props;
---

<script type="application/ld+json" is:inline set:html={safeJsonLd(data)} />
